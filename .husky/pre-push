# Run comprehensive checks before push (excluding e2e tests)
echo "🔍 Running pre-push checks..."
pnpm format
pnpm lint
pnpm typecheck
# Skip tests for mero-js package (e2e tests run only in CI)
pnpm --filter "!@calimero-network/mero-js" test
echo "✅ All pre-push checks passed!"
