name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - master

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

env:
  NODE_VERSION: '20'

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: lint-and-typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - name: Verify build artifacts
        run: |
          test -f dist/index.mjs
          test -f dist/index.cjs
          test -f dist/index.browser.mjs
          test -f dist/index.d.ts

  test-unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm test

  # E2E tests require a running merod node with the X-Auth-Error header fix.
  # Uncomment after the next core release includes the auth guard fix.
  # test-e2e:
  #   name: E2E Tests
  #   runs-on: ubuntu-latest
  #   needs: build
  #   timeout-minutes: 10
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'pnpm'
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm build
  #     - name: Start merod node
  #       run: |
  #         docker pull ghcr.io/calimero-network/merod:prerelease
  #         docker create --name merod-bin ghcr.io/calimero-network/merod:prerelease
  #         docker cp merod-bin:/usr/local/bin/merod ./merod
  #         docker rm merod-bin
  #         chmod +x ./merod
  #         ./merod --home ./e2e-node --node e2e init --server-port 4001 --swarm-port 4002 --auth-mode embedded 2>&1 || true
  #         RUST_LOG=warn ./merod --home ./e2e-node --node e2e run &
  #         echo $! > merod.pid
  #         for i in $(seq 1 30); do curl -sf http://localhost:4001/admin-api/health >/dev/null 2>&1 && break; sleep 1; done
  #         TOKEN=$(curl -sf http://localhost:4001/auth/token -H "Content-Type: application/json" \
  #           -d "{\"auth_method\":\"user_password\",\"public_key\":\"dev\",\"client_name\":\"http://localhost:4001\",\"timestamp\":$(date +%s),\"provider_data\":{\"username\":\"dev\",\"password\":\"dev\"}}" \
  #           | python3 -c "import json,sys; print(json.load(sys.stdin)['data']['access_token'])")
  #         curl -sf http://localhost:4001/admin-api/install-dev-application \
  #           -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  #           -d "{\"path\":\"$(pwd)/tests/e2e/assets/kv-store.mpk\",\"metadata\":[]}"
  #     - name: Run E2E tests
  #       run: npx vitest run --config vitest.e2e.config.ts tests/e2e/full-api.test.ts
  #       env:
  #         NODE_URL: http://localhost:4001
  #     - name: Stop node
  #       if: always()
  #       run: kill $(cat merod.pid) 2>/dev/null || true
