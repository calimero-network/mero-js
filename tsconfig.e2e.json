{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // The e2e suite lives outside src/, which the build's rootDir pins.
    "rootDir": "."
  },
  "include": ["tests/e2e/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
